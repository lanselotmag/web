server {	
		listen       80  default_server;
		# редирект uploads
		location ^~ /uploads {
                	                root /home/box/web;
        	}              
		#есть расширение
        	location ~* ^.+\..+$ {                                              
                	                root /home/box/web/public;                     
        	}
		#нет расширения
		location ~* ^((?!\.).)*$ {                                              
	                                return 404;
        	}	
}
